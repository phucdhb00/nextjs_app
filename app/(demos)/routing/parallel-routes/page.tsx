export default function ParallelRoutesPage() {
  return (
    <section className="card">
      <p>
        Parent route composes multiple slots concurrently. Useful for dashboard-like shells with
        independent refresh patterns.
      </p>
    </section>
  );
}
